{
  "title": "Website content checker",
  "description": "The actor tests given selectors on given pages and sends report about result.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "email": {
      "title": "Email",
      "type": "string",
      "description": "Email address where to send a report after the Actor finishes the testing",
      "prefill": "john@doe.xx",
      "editor": "textfield"
    },
    "urls": {
      "title": "URLs and selectors to test",
      "type": "array",
      "description": "Enter URLS and queries to check",
      "prefill": [{ "url": "http://www.apify.com", "userData": { "query": "footer #foot-tweet-text" } }],
      "editor": "requestListSources"
    }
  },
  "required": []
}
